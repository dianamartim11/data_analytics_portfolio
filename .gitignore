# Python
__pycache__/
*.pyc
.venv/
venv/
env/

# OS / editor junk
.DS_Store
Thumbs.db
.vscode/
.idea/

# Excel/Power BI temp lock files
~$*.xlsx
*.tmp

# Local SQLite database file (you regenerate it from schema.sql + the CSV)
*.db
*.sqlite

# You GENERATE data/processed/*_clean.csv by running 03_python/clean.py, and the
# chart PNGs by running the analysis scripts. Commit them once they exist so your
# Power BI, SQL loader and report can use them (they are not ignored).
